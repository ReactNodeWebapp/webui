import React from 'react';

import './Hero.scss';

function Hero(props) {
    return (
        <div className="hero" style={{height: props.height}}>
            
        </div>
    );
}

export default Hero;